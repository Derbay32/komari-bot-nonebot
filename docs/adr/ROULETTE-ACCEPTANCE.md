# 群绑定与轮盘最终验收记录

日期：2026-09-15，所有时间使用 Asia/Shanghai（UTC+8）。
责任：主代理独立核对实现、自动化证据与用户反馈；本次归档未委派。
范围：TSK-281 的绑定与轮盘集成交付；不表示已部署生产环境。

## 结论与人工口径

用户已确认正常双用户流程、道具反馈与入口、终局展示、正确胜者提及及本群排行榜通过。
本轮测试环境验收完成；没有额外的必需真人操作。

用户随后明确调整重复提及样本的验收口径：

> 后两类双人正常流程难以测试，提醒@都是相同代码，一个通过没必要测试多次。

因此使用本次终局胜者提及作为共用平台提及通道的代表性真人样本。
轮转、满仓奖励选择、成功上锁的目标选择和冻结载荷继续由自动化覆盖，不为共用通道重复随机开局。
这些分支没有单独的本轮真人确认；这不是把未实测写成实测，也没有删除或弱化测试断言。
该确认覆盖此前把每类提醒都当作必需真人样本的要求，不改变领域、权限或一次发送契约。

验收依据：

- 正式矩阵：TSK-268 评论 `6a9eebe01af305fd1c073f0e`，转正记录 `6a9eec861af305fd1c073f10`。
- 交互最终索引：TSK-266 评论 `6a9ec4831af305fd1c073eac`。
- 终局人工修订：TSK-266 评论 `6a9ece961af305fd1c073eb4`。
- 本轮口径调整：TSK-281 评论 `6aa83f9b62bf4279db592c13`。

## 版本与自动化证据

| 项目 | 已核对事实 |
|---|---|
| 实际运行代码 | `fd877325db66e42bbf5b4992bd0ca16f8a64dee1`，PR #166 合入 dev；GitHub 签名 verified/valid |
| 对应 CI HEAD | `412182fe8dbe30e599a54f8d531e67681d31fdbc`；合并树与 CI HEAD 完全一致 |
| 完整 CI | [run 34877512051](https://github.com/Derbay32/komari-bot-nonebot/actions/runs/34877512051)，两项必需检查成功 |
| 真实服务 | PostgreSQL 17 / Redis 7.4 隔离 CI：3686 passed，零 skipped，5 个既有 warnings |
| 无服务 | 3085 passed，593 skipped，8 deselected，3 个既有 warnings；不把门控跳过记为通过 |
| 静态与迁移 | Ruff、Pyright、upgrade head 与零漂移 check 通过 |
| 先前按钮修复 | PR #163/#164 已合入；合并版聚焦 34 项通过，完整真实服务 CI 3667 项通过 |
| 空面板修复 | PR #165 已合入；完整真实服务 CI 3672 项通过，用户确认通过 |

保留全部既有集成、并发、权限、故障、迁移及未知送达检查。
本次归档只修改文档，不修改业务、测试或 CI 配置；不因归档重新启动测试服务。

### 提及通道与场景差异

- `qq/renderer.py::_mention_tag` 统一构造 `<qqbot-at-user id="…" />`。
- `qq/delivery.py::build_qq_message` 统一将已冻结正文放入真实 QQ `MessageSegment.markdown`。
- `command_service.py::_reply_mention_target` / `_reply_mention_reason` 仍按场景选择单个目标。
- 轮转/奖励、锁目标、终局的正文插入位置不同；共用通道不等于所有场景代码完全相同。

以下检查已包含在上述真实服务全量 CI 中：

| 责任边界 | 自动化证据 |
|---|---|
| 目标及优先级、原地继续不提及 | `tests/komari_roulette/test_tsk278_fixture_probe.py::test_real_context_mention_priority_and_continue_in_place` |
| 下一玩家、奖励所属人、锁目标、胜者的唯一提及及位置 | `tests/komari_roulette/test_tsk278_renderer.py` |
| 真实事务后的上锁及满仓奖励视图 | `tests/komari_roulette/test_tsk278_renderer_pg.py` |
| 真实轮转目标、冻结正文和 QQ 消息载荷一致 | `tests/komari_roulette/test_tsk278_delivery_pg.py::test_real_delivery_preserves_frozen_mention_body` |
| 完整正常游戏与部分入库奖励、面板分层 | `tests/komari_roulette/test_tsk281_binding_to_game_chain_pg.py` |
| 首次取证、独立重绑、帮助隔离 | `tests/character_binding/test_tsk281_native_binding_chain_pg.py`、`test_tsk281_repair_then_rebind_pg.py`；`tests/komari_roulette/test_tsk281_help_isolation_pg.py` |

真实 PG 测试可在一次性隔离库设置库存或注入确定性随机，真实 QQ 对局没有使用这些手段。
平台 sender 是测试替身；自动化载荷正确不单独证明客户端展示或系统通知。
REST 组合用例使用真实路由及服务，但不是对当前测试应用开启管理 API 后的实机操作。
绑定代码从已实测版本 `58592f3` 到本次运行版本没有变化，复用适用的既有绑定反馈，不重复要求用户绑定。

## 本轮真人样本

| 样本 | 结果与边界 |
|---|---|
| 双用户独立绑定及会话归属 | 已确认；其他人使用会话命令被拒绝；重复确认修复已验收 |
| 开局、加入、开始及非当前玩家开枪拒绝 | 已确认；对应拒绝没有推进状态修订 |
| 空道具面板 | 用户确认新增提示通过；不表示抽奖发生 |
| 获奖提示与状态卡道具入口 | 用户确认通过；面板内继续使用道具，不把已有库存显示成新奖励 |
| 最终终局及胜者提及 | 用户确认完整普通正文、无引用/粗体/按钮及正确胜者对象通过；实际对局因开枪完成，不要求再以弃权重演相同提及通道 |
| 本群排行榜 | 用户确认展示正确；独立只读核对见下表 |
| 轮转、满仓奖励选择、成功上锁提醒 | 未单独获得本轮真人确认；依用户确认的代表性样本口径，由自动化覆盖场景差异，不阻塞验收 |
| 连发部分入库及全部道具组合 | 保留自动化边界证据；不扩称均已真人实测 |

本轮终局反馈未单列客户端系统与版本；不把先前 macOS 按钮排查扩大成终局的平台覆盖清单。
不推断未观察的静音、免打扰、离线推送、其他客户端或沙箱/正式账号条件。
不承诺任意客户端必然通知或消息永远送达。

## 运行与存储观测

| 时间 | 只读或已授权观测 |
|---|---|
| 02:03:12 | 更新后启动完成，QQ 与 OneBot 均连接；本次启动观测窗口 ERROR 为 0 |
| 02:14:57 | 最近对局 completed，reason=shot，turn_seq=2；不是因重载丢局 |
| 02:21:32 | 授权测试群有 4 份 completed 结果、2 个胜者榜项；逐成员汇总与榜单对照差异为 0 |

运行观测只说明对应窗口，不把“已连接”当作完整管理状态或无限期健康保证。
榜单对照只读取已完成结果，不读取未来弹序，不修改库存或累计胜场。
没有执行运营修复、真实 QQ 自动发送、历史消息重发、对局延期或强制终局。
凭据、原始身份和原始日志仍保存在仓库外的受限私有证据目录。
本记录不包含上述敏感数据。

## 交付与后续

原定实施链 TSK-271～280 已 Done；本记录完成 TSK-281 的最终归档。
父地图 TSK-258 的原设计与实施链已交付，不把历史原型通过替代本轮实现证据。
官 Bot 命令菜单发现性仍由 TSK-293 独立后续 Backlog 跟踪，不记作本轮已实现，不因验收收尾自动开始该任务。
测试服务继续运行，轮盘开关保持用户已授权的启用状态。
生产环境升级、菜单配置或运营通知仍需各自授权。
