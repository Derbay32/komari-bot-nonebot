# CHANGELOG

## [Unreleased]

> 当前 `dev` 分支上尚未发版的变更。

---

## [v1.2.0] - 2026-06-27

> **v1.2.0 正式版**

### 🚨 破坏性变更

- **好感度系统完全重构**：从旧每日随机/累计/历史记录改为单一当前值模型（范围 `[0, 400]`，默认 `100`），启动时检测旧结构后直接重建 `user_favorability` 表，旧好感度历史**不迁移**。
- **`.jrhg` 命令移除**：好感度只随聊天回复自动调整，不再提供独立查询命令。
- **`llm_provider` 全局 `reasoning_effort` 字段删除**：改为各业务插件 per-call 配置（如 `llm_thinking_mode_chat`、`summary_thinking_mode` 等）。
- **旧用户属性 API 移除**：`UserAttribute`、`get_user_attribute()`、`set_user_attribute()` 等全部删除。
- **记忆向量表拆分**：`komari_memory_conversations` 与 `komari_memory_interaction_history` 不再内联 `embedding` 列，改由独立外键表保存向量。

### ✨ 新增功能

#### 聊天与交互
- **表情反应提示**：判定回复后自动 `set_msg_emoji_like`，示意生成中。
- **联网搜索**：`komari_search` 封装 Tavily Search API，提供 `search_web` 工具调用。
- **V4 多模态**：工具调用支持读取图片，实现图文混合对话。
- **`final_response` 终端工具**：所有回复通过 `tool_choice="required"` 强制结构化输出，互动历史同步生成。
- **`read_profile` 只读工具**：接入聊天默认工具集。

#### 知识库与社区
- **知识库提案插件**（`komari_custom`）：`.custom` 指令组 → 多步编辑 → 投票通过 → 自动写入知识库。

#### 记忆系统
- **跨群互动事件记忆**：Redis 缓冲 → LLM 跨群聚合（禁止输出群号/群名） → PG 向量存储，后台 Worker 自动处理。
- **画像 Agent 化**：LLM tool calling 模式（read/write/preview/count/commit_profile），Redis 暂存 + PG advisory lock 互斥。
- **四阶段好感度提示词**：统一 `0-99`、`100-199`、`200-299`、`300-400` 阶段定义，LLM delta 工具强制记录好感度变化。

#### 基础设施
- **判定场景 PostgreSQL 内容表迁移**：`komari_decision_scenes` 作为唯一运行时 source of truth，配套管理 API。
- **配置系统迁移**：`config_manager` 从本地 JSON 迁移到 PG `komari_plugin_configs`；Prompt 存储独立表 `komari_prompt_configs`。
- **消息缓冲重构**：空闲超时 + 最小消息数替代 token 计数触发总结。

### 🔧 改进

#### 思考模式
- **`thinking_mode` / `reasoning_effort` per-call 化**：`komari_memory`、`group_history_summary`、`llm_provider` 新增独立思考参数配置，`thinking_mode=True` 时自动跳过 `tool_choice` 注入。
- **视觉模型思考参数透传**：聊天循环中 `has_vision_tool=True` 时按视觉模型独立配置选择思考参数。

#### 画像与记忆
- **画像写入载荷语义调整**：内部改用 `set_traits` / `delete_keys` patch，完整 profile payload 只表示"设置本次出现的 key"，不再隐式删除缺失 key。
- **`user_data` 好感度服务收敛**：运行时代码只保留 `user_favorability` 当前好感度能力。
- **记忆向量读写路径重构**：对话记忆与跨群互动事件改为事务内维护独立 embedding 表。
- **向量迁移脚本独立化**：`scripts/migrate_embeddings.py` 不再导入 `komari_bot` 包。

#### Chat Prompt
- **首轮只注入当前用户画像**，移除冗余块。
- **YAML 风格格式化**：画像/关键词知识/可见用户/Redis 近期互动/PG 互动事件记忆统一格式。
- **预读取优化**：构建 prompt 前预读 PG 画像、Redis 近期互动缓冲（限 10 条）、PG 长期互动事件记忆。

#### 工具执行
- **重构工具执行循环**：将硬失败改为发送纠错重试消息，抽取工具辅助函数。
- **工具循环合并**：视觉/搜索工具统一为 `_execute_tool_loop()`，消除 ~190 行重复代码。

#### 其他
- **默认总结模型**：`llm_model_summary` 从 `gemini-2.5-flash-lite` 改为 `deepseek-v4-flash`。
- **对话总结幂等**：`dedup_key` + partial unique index，`ON CONFLICT DO NOTHING`。

### 🛡️ 安全加固

- **图片下载 SSRF 防护**：校验 URL、DNS 解析结果与每一跳重定向，阻断内网/环回/链路本地/保留地址。
- **聊天 Prompt 注入隔离**：历史消息、引用消息、当前输入等不可信数据统一转义并包裹边界。
- **管理配置 API 凭据脱敏**：token/password/secret/key/credential 字段统一返回 `******`。
- **配置更新日志安全**：`ConfigManager.update_field()` 日志仅记录插件名与字段名。
- **LLM 日志保存安全**：对日志记录结构化与敏感内容进行脱敏处理。
- **ILIKE 查询注入防护**：新增 `escape_like_pattern()` 统一转义 `\`、`%`、`_`。
- **`komari_custom` 异常泄露修复**：兜底异常提示改为固定安全文案。

### 🐛 修复

#### 并发与竞态
- **用户画像 JSONB 增量写入**：改为 PostgreSQL 侧原子 patch，避免并发覆盖。
- **画像 Agent 提交竞态**：`ProfileStaging.commit()` 关闭 TOCTOU 窗口。
- **`user_data.get_db()` 初始化竞态**：新增 `asyncio.Lock` 双重检查。
- **`ConfigStorage._run()` 同步桥接无超时**：新增 5 秒超时。
- **`ConfigStorage._get_pool()` 初始化失败残留 pool**：schema 初始化失败时关闭临时连接池。
- **LLM / Vision / Tavily 外部调用并发控制**：模块级 `asyncio.Semaphore` 限制并发。

#### 崩溃与稳定性
- **昵称 fallback 格式化崩溃**：移除 `:.0f` 格式符。
- **`.sr` 空列表崩溃**：检查空列表并提前 finish。
- **`user_data.database` 连接池运行时 assert**：新增 `_require_pool()` 私有方法。
- **记忆遗忘批处理单任务异常中断整批**：修复单任务异常直接中断整批的问题。
- **总结时过高并发导致 429 错误**：引入总结服务、聊天服务全局 RPM 上限。

#### API 与配置
- **Scene API 回退路径重复执行 DDL**：重构场景仓储获取逻辑。
- **公告 API 无组数量限制与限流**：引入发送限流与可配置参数。
- **profile 批量 upsert 一个冲突会回滚整批**：重构为逐条隔离事务。
- **LLM Provider 模块名修正**：DeepSeek 专属命名泛化为 OpenAI 兼容 API 统一接口。
- **配置/Prompt 存储 shutdown hook**：新增 `close_config_storage_if_created()` / `close_prompt_storage_if_created()`。

#### 其他修复
- **旧互动历史写入路径**：停用旧 JSONB 写入入口。
- **`.custom` 编辑缓冲污染**：移除回复引导消息自动追加入口，新增 `.custom append <文本>` 显式追加。
- **Tavily 查询长度限制与短期缓存**：query 截断至 200 字符，60 秒进程内缓存。
- **思考模式与 `tool_choice` 参数冲突**：`thinking_mode=True` 时自动跳过 `tool_choice` 注入。
- 陈旧测试断言、对话总结重试重复写入、时间戳泄漏、PromptStorage 超时/init 不可重试、API 异常信息泄露。

### 🗑️ 移除

- **`.jrhg` 今日好感插件与命令**。
- **`komari_healthcheck` 插件**（健康检查已迁移到 napcat 侧）。
- **`nonebot-plugin-localstore` 依赖**。
- **JSON 配置示例文件**。
- **XML 标签提取**、旧 dispatch 回调、旧模板字段、分段/合并机制、`summary_chunk_token_limit` 配置。
- **旧用户属性 API 与清理配置**：`UserAttribute`、相关 getter/setter、`data_retention_days` 与 APScheduler 属性清理任务。
- **`komari_status` 状态查询插件**。
- **独立 LLM 压缩路径**、JSON 操作数组旧模式、离线瘦身脚本。
- **`llm_provider` 全局 `reasoning_effort` 字段**。

---

## [v1.2.0-rc.7] - 2026-06-25

### Changed
- 重构工具执行循环，将硬失败改为发送纠错重试消息，并抽取工具辅助函数。

---

## [v1.2.0-rc.6] - 2026-06-17

### Fixed
- **思考模式与 `tool_choice` 参数冲突**：`thinking_mode` / `reasoning_effort` 从 `llm_provider` 全局字段改为各业务插件 per-call 配置，`thinking_mode=True` 时自动跳过 `tool_choice` 注入；`deepseek-v4` 系模型 `thinking_mode=False` 时注入 `thinking:disabled` 关闭默认思考。涉及 `komari_memory`、`komari_chat`、`group_history_summary` 三个插件的 15 处 LLM 调用点透传。

### Changed
- **`thinking_mode` / `reasoning_effort` per-call 化**：`komari_memory` 新增 `llm_thinking_mode_chat` / `llm_reasoning_effort_chat` / `llm_thinking_mode_summary` / `llm_reasoning_effort_summary`；`group_history_summary` 新增 `summary_planning_thinking_mode` / `summary_planning_reasoning_effort` / `summary_thinking_mode` / `summary_reasoning_effort`；`llm_provider` 新增 `vision_thinking_mode` / `vision_reasoning_effort`。所有默认值为 `False` / `""`，未配置时行为与改造前完全一致。
- **视觉模型思考参数透传**：`message_handler` → `generate_reply_with_tools` → `_execute_tool_loop` 链路新增 `vision_thinking_mode` / `vision_reasoning_effort` 参数，聊天循环中 `has_vision_tool=True` 时按视觉模型独立配置选择思考参数。

### Removed
- **`llm_provider` 全局 `reasoning_effort` 字段**（破坏性变更）：`DynamicConfigSchema` 与 `Config` 中的 `reasoning_effort` 字段被删除。若管理员曾通过管理 API 设置过全局 `reasoning_effort`，需迁移到各业务插件的 `*_reasoning_effort` 字段。

---

## [v1.2.0-rc.5] - 2026-06-14

### Fixed
- **总结时过高并发导致出现 429 错误**：引入总结服务、聊天服务全局 RPM 上限，避免因过高并发请求导致出现 429 错误。

---

## [v1.2.0-rc.4] - 2026-06-12

### Fixed
- **Scene API 回退路径重复执行 DDL**：重构场景仓储获取逻辑，避免强制加载决策插件并缓存回退实例。
- **公告 API 无组数量限制与限流**：为维护通知接口引入发送限流与可配置参数。
- **profile 批量 upsert 一个冲突会回滚整批**：重构用户画像批量写入为逐条隔离事务并支持部分失败处理。
- **LLM 日志保存安全问题**：对日志记录结构化与敏感内容进行脱敏处理。
- **LLM Provider 模块名修正**：fix(llm_provider): 将 DeepSeek 专属命名泛化为 OpenAI 兼容 API 统一接口以反映真实职责。

---

## [v1.2.0-rc.3] - 2026-06-10

### Fixed
- **昵称 fallback 格式化崩溃**：`get_user_nickname()` 移除 `:.0f` 格式符，字符串用户 ID 不再引发 `ValueError`。
- **`.sr` 空列表崩溃**：主指令读取 sr_list 后检查空列表并提前 finish，避免 `randint(0, -1)` 崩溃。
- **`user_data.get_db()` 初始化竞态**：新增 `asyncio.Lock` 双重检查，防止并发初始化创建多余连接池；初始化失败时保留 `_db = None`，允许下次重试。
- **`user_data.database` 连接池运行时 assert**：新增 `_require_pool()` 私有方法，统一替换三处 `assert self._pool is not None`，生产环境不再依赖 `-O` 标志。
- **`ConfigStorage._run()` 同步桥接无超时**：新增 5 秒超时，超时时 cancel future 并抛 `RuntimeError`。
- **`ConfigStorage._get_pool()` 初始化失败残留 pool**：schema 初始化失败时关闭临时连接池，不留下半初始化 pool。
- **配置/Prompt 存储 shutdown hook**：新增 `close_config_storage_if_created()` / `close_prompt_storage_if_created()`，分别在 `config_manager` 与 `komari_management` 插件入口注册 NoneBot `on_shutdown` 钩子，避免 shutdown 阶段创建新资源。
- **LLM / Vision / Tavily 外部调用并发控制**：模块级 `asyncio.Semaphore` 限制 LLM completion（4）、视觉模型读取（2）、Tavily 搜索（2），避免单进程内突发打满外部 API。
- **Tavily 查询长度限制与短期缓存**：query 截断至 200 字符；60 秒进程内缓存，key 包含 `query`/`search_depth`/`max_results`/`include_answer`/`result_content_limit` 且不含 API key；异常响应不写入缓存。
- **管理配置 API 凭据脱敏**：配置详情、reload 与 patch 响应中的 token/password/secret/key/credential 字段统一返回 `******`，避免管理接口明文泄露密钥。
- **配置更新日志安全**：`ConfigManager.update_field()` 日志仅记录插件名与字段名，不再输出敏感或普通配置的新值。
- **图片下载 SSRF 防护**：图片下载器移除 `data:` 输入下载路径，下载前校验 URL、DNS 解析结果与每一跳重定向，阻断内网、环回、链路本地、保留地址等目标并限制重定向次数。
- **聊天 Prompt 注入隔离**：历史消息、引用消息、当前输入、记忆、知识库、画像、互动历史与好感度上下文统一转义并包裹不可信数据边界；对话总结 prompt 同步转义外部文本并使用 `ConversationSummarySchema` 校验 LLM JSON 输出。
- **修复komari_custom 用户侧暴露内部异常**：将兜底异常提示从 `❌ 处理请求失败：{e}` 改成固定安全文案 `❌ 处理请求失败，请稍后再试`。
- **记忆遗忘批处理单任务异常中断整批**：修复记忆遗忘批处理时，单任务异常会直接中断整批的问题。
- **ILIKE 查询存在通配符模式注入**：新增 `escape_like_pattern()` 统一转义 \、%、_ ，并修复所有计划内 ILIKE 搜索。

---

## [v1.2.0-rc.2] - 2026-06-09

### Fixed
- **用户画像 JSONB 增量写入**：`komari_memory_user_profile.traits` 改为 PostgreSQL 侧原子 patch，避免并发提交不同 trait key 时后写覆盖前写。
- **画像 Agent 提交竞态**：`ProfileStaging.commit()` 将 snapshot 基线时间传入 `ON CONFLICT DO UPDATE ... WHERE`，关闭预检查与最终写入之间的 TOCTOU 窗口。
- **旧互动历史写入路径**：停用旧 `komari_memory_interaction_history.records` JSONB 写入入口，避免废弃整列覆盖路径继续误导维护。
- **`.custom` 编辑缓冲污染**：移除回复引导消息自动追加入口，新增 `.custom append <文本>` 显式追加标题/正文，避免普通回复、引用消息或命令文本误写入 Redis session。

### Changed
- **画像写入载荷语义**：`MemoryService` / `EntityRepository` 保留原入口名称，内部改用 `set_traits` / `delete_keys` patch；完整 profile payload 只表示“设置本次出现的 key”，不再隐式删除缺失 key。
- **`user_data` 当前好感度服务收敛**：运行时代码只保留 `user_favorability` 当前好感度能力，`get_user_count()` 改为只统计当前好感度表；新增 `scripts/drop_legacy_user_attributes_table.py` 供维护者显式删除旧 `user_attributes` 表。
- **记忆向量表拆分**：`komari_memory_conversations` 与 `komari_memory_interaction_history` 不再内联 `embedding` 列，改由 `komari_memory_conversation_embeddings` / `komari_memory_interaction_embeddings` 通过外键保存向量、内容哈希与维度元数据；schema 初始化会删除旧主表向量列并重建新向量索引。
- **记忆向量读写路径**：对话记忆与跨群互动事件的创建、更新、相似度搜索均改为事务内维护独立 embedding 表，搜索通过 join 新表并保持上层返回结构不变。
- **向量迁移脚本独立化**：`scripts/migrate_embeddings.py` 不再导入 `komari_bot` 包，配置仅从命令行参数和环境变量读取；`--target memory` 会分别重建 conversation 与 interaction 两类历史向量，`knowledge` 目标语义保持不变。

### Removed
- **旧用户属性 API 与清理配置**：移除 `UserAttribute`、`get_user_attribute()`、`set_user_attribute()`、`get_user_attributes()`、`cleanup_old_attributes()`、`data_retention_days` 与 APScheduler 属性清理任务注册逻辑。

---

## [v1.2.0-rc.1] - 2026-06-08

### 判定场景 PostgreSQL 内容表迁移
- **`komari_decision_scenes` 内容主表**：独立 PG 表作为 scene 唯一运行时 source of truth，`scene_id` 外键关联向量快照表。
- **`PostgresSceneTemplateLoader`**：运行时 scene sync 默认从 PG 内容表构建，替代旧 YAML 文件哈希。
- **管理 Scenes API**：`/api/komari-decision-scenes/v1` 提供 list/detail/PUT/PATCH/sync 端点。
- **迁移脚本**：`scripts/migrate_komari_decision_scenes_to_pg.py`，支持 `--source-path` / `--dry-run` / `--replace-existing`。
- **`UnifiedCandidateRerankService` 去除全部 YAML fallback 路径**，无 snapshot 时报错不再降级。

### 跨群互动事件记忆系统
- **事件向量表**：`komari_memory_interaction_history` 替换为按 `user_id` 分隔的事件向量表，启动时自动旧表重命名。
- **Redis 跨群互动缓冲**：`global_interaction:{user_id}` key，20 条触发 pending，Lua RENAME 原子转移。
- **CRUD 与总结**：`InteractionEventRepository` 向量检索/分页/touch/模糊化；LLM 跨群聚合（禁止输出群号/群名）。
- **后台 Worker**：轮询 pending → LLM 总结 → 嵌入 → 写 PG，失败重试 3 次后恢复快照。每日 4:00 兜底扫描。

### 画像 Agent 化
- **Agent 工具调用**：画像总结改为 LLM tool calling 模式（read/write/preview/count/commit_profile），硬校验 trait 上限。
- **Redis 暂存层**：`ProfileStaging` 暂存到 Redis（TTL 10 分钟），冲突检测后 commit() 写库。
- **PG 生命周期锁**：`memory_agent_locks.py` 按 `group_id` 通过 PG advisory lock 互斥。
- **显式提交**：必须 `commit_profile` 才写库，超限返回 `limit_exceeded` 供 LLM 压缩后重试。
- **移除**：独立 LLM 压缩路径、JSON 操作数组旧模式、离线瘦身脚本。

### 聊天回复工具调用结构化
- **`final_response` 终端工具**：所有回复通过 `tool_choice="required"` 强制结构化输出。
- **互动历史同步生成**：每次回复同步生成 `interaction_history`（event/result/emotion），不再依赖 XML 标签解析。
- **工具循环合并**：视觉/搜索工具统一为 `_execute_tool_loop()`，消除 ~190 行重复代码。

### Chat Prompt 上下文优化
- **首轮只注入当前用户画像**，移除 `<user_profiles>` / `<user_interaction_history>` 块。
- **YAML 风格格式化**：画像/关键词知识/可见用户/Redis 近期互动/PG 互动事件记忆统一 YAML 风格注入。
- **新增 `read_profile` 只读工具**，`read_profile` 接入 `_attempt_reply()` 默认工具集。
- **`<user_profiles>` 重命名 → `<user_keyword_knowledge>`**。
- **预读取优化**：构建 prompt 前预读 PG 画像、Redis 近期互动缓冲（限 10 条）、PG 长期互动事件记忆。

### 好感度系统重构
- **当前值模型**：`user_data` 好感度从旧每日随机/累计/历史记录改为单一当前值，范围 `[0, 400]`，默认初始值 `100`。
- **破坏性表重建**：启动时检测旧 `daily_favor` / `cumulative_favor` / `last_updated` 结构后直接重建 `user_favorability`，旧好感度历史不迁移。
- **四阶段提示词**：统一 `0-99`、`100-199`、`200-299`、`300-400` 阶段定义，聊天 prompt 注入 `<favorability_stage>` 替代旧 `<favorability_modifier>`。
- **LLM delta 工具**：新增并强制 `record_favorability_delta`，模型必须先记录本轮好感度变化再 `final_response`；业务代码仅在回复成功后提交 delta，避免重试重复写库。
- **`user_data` API 更新**：新增/保留 `get_user_favorability()`、`adjust_user_favorability()`、`get_favorability_stage()`，移除旧每日生成、历史查询与格式化回复 API。
- **迁移工具精简**：`tools/migrate_user_data_sqlite_to_pg.py` 只迁移 `user_attributes`，不再迁移旧好感度历史。

### 配置系统迁移
- **配置持久化**：`config_manager` 从本地 JSON 迁移到 PG `komari_plugin_configs` 表。
- **Prompt 存储**：`common/prompt_storage.py` 独立表 `komari_prompt_configs`，管理 Prompt API 读写该表。
- **旧 YAML prompt 迁移脚本**：`scripts/migrate_prompt_config_to_pg.py`。

### 总结模型与 Agent 工作流
- **默认模型**：`llm_model_summary` 从 `gemini-2.5-flash-lite` 改为 `deepseek-v4-flash`。
- **Agent 路径分叉**：画像走 `run_profile_agent()` → 暂存 → 提交；对话总结纯 `summarize_conversation()`。
- **对话总结幂等**：`dedup_key` + partial unique index，`ON CONFLICT DO NOTHING`。

### Removed
- **`.jrhg` 今日好感插件与命令**，好感度只随聊天回复自动调整，不再提供独立查询命令。
- **`komari_healthcheck` 插件**（健康检查已迁移到 napcat 侧）。
- **`nonebot-plugin-localstore` 依赖**。
- **JSON 配置示例文件**。
- **XML 标签提取**、旧 dispatch 回调、旧模板字段、分段/合并机制、`summary_chunk_token_limit` 配置。

### Fixed
- 陈旧测试断言、对话总结重试重复写入、时间戳泄漏、PromptStorage 超时/init 不可重试、API 异常信息泄露。
- 修复无法加载旧 scene set 时，不会自动重建 embedding 的问题

---

## [v1.2.0-beta.4] - 2026-05-28

### Added
- **表情反应提示**：`komari_chat` 判定回复后自动 `set_msg_emoji_like`，示意生成中。`face_reaction_enabled` / `face_reaction_id` 配置。
- **联网搜索**：`komari_search` 封装 Tavily Search API，`search_web` 工具调用。
- **V4 多模态**：工具调用读取图片，图文混合对话。
- **知识库提案插件**：`komari_custom`，`.custom` 指令组 → 多步编辑 → 投票通过 → 自动写入知识库。
- **消息缓冲重构**：空闲超时 + 最小消息数替代 token 计数触发总结。

### Changed
- 提案提交者展示名称与 QQ 号；KnowledgeCategory 新增 `"custom"` 分类；移除 `komari_status` 状态查询。

### Fixed
- 总结请求误拦截（误用 meaningful/noise_score）；GitHub Action v 前缀修复。

---

## [v1.1.0] - 2026-04-19

> **首个正式发布的 v1.1 稳定版**。

### Added
- **配置管理 REST API**；DeepSeek `extra_params` 自定义参数支持。
- **工具调用能力**：`llm_provider` 统一接口接入工具调用；群聊总结插件同步接入。
- **实体增量更新**：从全量改写改为增量；忘却模糊化并发优化。
- **角色名绑定**：总结前查询群内绑定；提示词模板化。
- **维护通知推送与群组查询 API**；状态监控插件 `komari_status`（Uptime Kuma 集成）。

### Changed
- **LLM 接口迁移**：从自建客户端迁移至 `openai` 库。
- **插件解耦**：`decision` / `memory` 使用 NB2 标准插件间引用；`jrhg` 好感度系统迁移至 `komari_chat`。
- **`komari_help` 命令重命名 → `docs`**，新增分页、`max_reply_result_count`、`disabled_auto_generate_plugins` 配置。
- **数据库配置统一**：Redis 配置移至共享层；各插件数据库覆盖机制移除。
- **数据库增量写入** + 迁移脚本；日志级别下调。

### Fixed
- 早期 Message/OneBot 类型注解问题；`response_format` 兼容性；图片下载器 EOF/重试；Sentry 初始化。

### Removed
- 本地向量支持、内置 WebUI、过时 `not_related` 空回功能。

---

## [v1.0.0] - 2026-03-31

> **首个稳定版发布**。

### Core
- **NoneBot2** + **OneBot V11**，QQ 机器人基础架构。
- **18 个插件**：基础服务层 / 核心功能层 / 辅助功能层。

### Major Features
- **AI 聊天**（`komari_chat`）：LLM 驱动对话，支持 @触发、主动回复。
- **四层记忆系统**（`komari_memory`）：对话摘要、用户画像、互动历史、实体知识。
- **RAG 知识库**（`komari_knowledge`）：向量检索 + 关键词匹配，REST 管理 API。
- **智能帮助**（`komari_help`）：基于向量检索的帮助文档。
- **群聊总结**（`group_history_summary`）：@触发自动总结，图片输出。
- **好感度系统**（`.jrhg`）、角色绑定（`.nn`）、神人榜（`.sr`）。
- **运维**：Sentry 集成、Bark 推送、健康检查。

### Infrastructure
- **PostgreSQL + pgvector**（HNSW 索引，raw SQL）、**Redis 7.1+**（`redis.asyncio`）。
- **FastAPI** 内嵌管理 API、**Docker Compose** 部署、**GitHub Actions** CI/CD。
- Python **3.13+**，Pyright `standard` + Ruff 零容忍类型错误。

---

## [v0.7.0] - 2026-03-05

> v1.0.0 之前的最后一个 milestone。

- 知识库注入（RAG + 关键词检索）；四层记忆系统初版。
- AI 聊天增强（BERT 主动回复判定）；`.nn` / `.sr` 复刻。
- Bot 上下线 Bark 推送；权限/配置/数据库管理插件独立。
- Docker 部署优化；统一数据库接入；提示词持续优化。

---

## 版本对照表

| 版本 | 日期 | 类型 |
|------|------|------|
| Unreleased | - | dev 分支 |
| v1.2.0 | 2026-06-27 | **Stable** |
| v1.2.0-rc.7 | 2026-06-25 | RC |
| v1.2.0-rc.6 | 2026-06-17 | RC |
| v1.2.0-rc.5 | 2026-06-14 | RC |
| v1.2.0-rc.4 | 2026-06-12 | RC |
| v1.2.0-rc.3 | 2026-06-10 | RC |
| v1.2.0-rc.2 | 2026-06-09 | RC |
| v1.2.0-rc.1 | 2026-06-08 | RC |
| v1.2.0-beta.4 | 2026-05-28 | Beta |
| v1.2.0-beta.3 | 2026-04-25 | Beta |
| v1.2.0-beta.2 | 2026-04-23 | Beta |
| v1.2.0-beta.1 | 2026-04-21 | Beta |
| v1.1.0 | 2026-04-19 | **Stable** |
| v1.1.0-rc.2 | 2026-04-18 | RC |
| v1.1.0-rc.1 | 2026-04-18 | RC |
| v1.1.0-beta.7 | 2026-04-14 | Beta |
| v1.1.0-beta.6 | 2026-04-13 | Beta |
| v1.1.0-beta.5 | 2026-04-12 | Beta |
| v1.1.0-beta.4 | 2026-04-12 | Beta |
| v1.1.0-beta.3 | 2026-04-11 | Beta |
| v1.1.0-beta.2 | 2026-04-09 | Beta |
| v1.1.0-beta.1 | 2026-04-08 | Beta |
| v1.0.0 | 2026-03-31 | **Stable** |
| v0.7.0 | 2026-03-05 | Milestone |

---

*本 CHANGELOG 最后更新于 2026-06-27*
