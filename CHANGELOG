# CHANGELOG

## [Unreleased]

> 当前 `dev` 分支上尚未发版的变更。本次更新围绕三大主题：**跨群互动事件记忆**（重构互动历史为跨群事件向量表）、**画像 Agent 化**（LLM tool calling 驱动的画像管理）、**聊天回复工具调用结构化**（统一 `final_response` 工具终结回复流程）。

### Added

- **跨群用户长期互动事件记忆**：将 `komari_memory_interaction_history` 从旧 `(user_id, group_id)` 主键 JSONB records 表替换为按 `user_id` 分隔的事件向量表，每行存储一条由 LLM 总结出的跨群互动事件记忆，支持 `event_summary` 向量检索与整数忘却模型。
- **schema 自动迁移**：`vector_storage_schema.py` 启动时自动检测旧表形态（存在 `records` JSONB 列且 `group_id`），自动重命名为 `_legacy_<时间戳>` 并创建新事件表，不破坏旧数据。
- **Redis 跨群互动缓冲**：新增 `global_interaction:{user_id}` key，`RPUSH` 写入原始互动记录（不含 `group_id`），20 条仅触发 pending 不做 `LTRIM`；快照使用 Lua `RENAME` 原子转移 buffer → processing key，写入新 buffer 不受影响。
- **`InteractionEventRepository`**：新事件向量表 CRUD，包含按 `user_id` 固定过滤的向量检索、分页列出、`touch_interaction_events()` 刷新（同时更新 `last_accessed` 并恢复 `importance_current = importance_initial`）、模糊化更新。
- **`summarize_interaction_events()`**：LLM 总结服务，将一批原始互动跨群记录聚合为单条事件记忆，要求 JSON 输出且禁止出现群号/群名。
- **后台 Worker**：`interaction_event_worker.py` 轮询 pending → 快照转移 → LLM 总结 → 嵌入 → 写 PG → 删除 processing key；失败复用 `retry_async` 重试 3 次，仍失败将 processing 快照恢复回原 buffer 并保持时间顺序。每日 4:00 兜底扫描非空 buffer 加入 pending。
- **管理 API `/interactions`**：新增事件语义路由 `GET /interactions`、`GET /interactions/{event_id}`、`PATCH /interactions/{event_id}`、`DELETE /interactions/{event_id}`。
- **配置项**：`global_interaction_enabled`、`global_interaction_trigger_size`（默认 20）、`global_interaction_summary_interval_minutes`（默认 1）。
- **实现即 touch 测试覆盖**：测试验证了 Redis 缓冲不截断、快照恢复顺序、事件召回后自动 touch 刷新、`/interactions` API CRUD 与校验。
- **画像 Agent 化**：用户画像总结从旧 JSON 操作数组改为 LLM tool calling Agent 模式。新增 `read_profile`、`write_profile`、`preview_profile`、`count_profile_traits`、`commit_profile` 工具供 LLM 按需操作，程序侧在 `commit_profile` 提交前硬校验 trait 上限，避免"忘记 commit / 错误 commit / commit 后继续写"的不确定性。
- **Redis 暂存层**：新增 `ProfileStaging`，画像操作先暂存到 Redis（`komari_memory:staging:profile:{session_id}`，TTL 10 分钟），冲突检测 + 规范化合并后通过 `commit()` 写入数据库，异常时 `discard()` 自动清理。
- **PG Agent 生命周期锁**：新增 `memory_agent_locks.py`，画像 Agent 按 `group_id` 通过 PostgreSQL advisory lock 互斥，同群并发 Agent 后发起者阻塞等待前一个完成；后续互动历史 Agent 可按 `(group_id, user_id)` 复用同一锁库。
- **画像操作纯函数层**：新增 `profile_operations.py`（`ProfileOperation`、`ProfileDiffItem`、`StageResult`、`CommitResult` 等），`normalize_profile_operations()` 实现 add/set/delete 多轮规范化合并，`apply_profile_operations()` 将最终 diff 应用到画像 JSON。
- **Agent 预算控制**：新增配置项 `memory_agent_max_rounds`、`memory_agent_max_tool_calls`、`memory_agent_max_read_profiles`、`memory_agent_max_write_operations`、`memory_agent_lock_timeout_seconds`，超出预算自动 discard 并告警。
- **`count_profile_traits` / `commit_profile` 工具**：新增画像计数工具（支持 `include_staged` 纳入暂存区修改）与显式提交工具。`commit_profile` 在真实写库前对所有受影响用户执行 `profile_trait_limit` 硬校验，超限返回 `limit_exceeded`（含超限用户、数量、当前 traits、trait_keys），保留 Redis 暂存区供 LLM 继续压缩后重试，不写 PostgreSQL。
- **`ProfileReadResult.display_name`**：`read_profile` 工具返回值补充 `display_name` 字段，`commit_profile` 的 `limit_exceeded.violations` 也携带，减少 LLM 为确认超限用户身份的额外查询。
- **`final_response` 工具**：新增内部终端工具 `final_response`，所有聊天回复（含普通回复与工具回复）均通过 `tool_choice="required"` 强制模型调用以输出结构化结果。
- **互动历史同步生成**：聊天回复与互动历史绑定在同一结构化输出中，每次回复都同步生成 `interaction_history`（包含 event / result / emotion），不再依赖 XML 标签文本解析。
- **`ReplyResult` / `InteractionHistoryRecord`**：新增结构化返回值类型，`generate_reply()` 与 `generate_reply_with_tools()` 返回 `ReplyResult`（含 content + interaction_history）。
- **`_write_interaction_history()`**：新增互动历史写入方法，复用 `MemoryAgentLockScope.INTERACTION_USER`，在 PG advisory lock 内完成 read-modify-write，自动裁剪最近 10 条记录，写入失败不阻塞回复发送。

### Changed

- **`_write_interaction_history()`**：`komari_chat` 消息处理器中的互动历史写入改为写 Redis 跨群缓冲，不再读取旧 PG records、不再持有 advisory lock、不写 `group_id`；增加 `global_interaction_enabled` 开关控制。
- **`MemoryService`**：注入 `InteractionEventRepository`；新增 `insert_interaction_event()`、`search_interaction_events()`（内置 touch 刷新）；旧 `get_interaction_history()` 等 old API 安全返回 None。
- **`ForgettingService`**：对新事件表接入整数衰减、低价值删除、高价值首次归零模糊化与二次归零删除。
- **`PluginManager`/`__init__.py`**：初始化和注册 interaction event repository 与 worker，并增加新表 embedding 维度校验。
- **总结模型默认值**：`llm_model_summary` 从 `gemini-2.5-flash-lite` 改为 `deepseek-v4-flash`（需 function calling 支持）。
- **总结提示词拆分**：新增 `memory_summary_common_system`（画像/总结共用前缀）与 `profile_agent_workflow_system`（画像 Agent 专用工具流程）。后续 `summary_prompt` / `merge_prompt`、旧互动历史相关模板字段及未使用的画像提交提醒模板均已移除。
- **`summary_worker` 路径分叉**：画像路径走 `run_profile_agent()` → Agent + 暂存 + 自动提交；互动路径原保留旧 `summarize_conversation()` → JSON 解析 + `_apply_interaction_operations()`，后续已移除。
- **记忆总结共用前缀缓存**：`summarize_conversation()` 改为三段式 `messages`（system + user + workflow），前两条与画像 Agent 完全一致以命中 LLM 服务端前缀缓存；输出从旧操作数组合并改为纯 `{"memories": [{"content": ..., "importance": ...}]}` 多段对话记忆，不再产出 `user_profile_operations` / `user_interaction_operations`。
- **总结三层 fallback**：总结采用 JSON mode → 强制 `output_summary_result` tool calling → markdown JSON 直出解析的三层结构化输出降级链，并修复 `DeepSeekClient` 对 `response_format` 的请求体透传。
- **总结执行顺序调整**：`perform_summary()` 先执行总结并逐条 `store_conversation()` 写入 PG，再运行画像 Agent；总结失败会记录异常但不阻断画像后续流程。
- **画像提交改为显式**：`run_profile_agent()` 不再在 LLM 停止工具调用后自动提交，改为必须由 Agent 显式调用 `commit_profile` 才写库；未成功提交或超过轮数结束会自动丢弃暂存区返回 `discarded` / `nothing_to_commit`。
- **画像压缩整合进 Agent**：压缩从独立的 LLM 文本 JSON 路径改为 Agent 工具调用循环内完成。LLM 通过 `count_profile_traits(include_staged=true)` 检测超限 → `write_profile`（delete/set）压缩 → `count_profile_traits` 复查 → `commit_profile` 提交；`limit_exceeded` 失败直接根据 violations 继续修正重试。
- **提示词工作流更新**：`profile_agent_workflow_system` 明确计数、压缩、预览、显式提交和超限重试流程；删除未使用的 `profile_agent_commit_reminder`、`profile_compaction_agent_system`、`profile_compaction_commit_reminder` 模板。
- **聊天回复工具调用合并**：合并 `generate_reply_with_vision_tool` 与 `generate_reply_with_search_tool` 为统一的 `generate_reply_with_tools()`，由调用方显式传入 `tools` 列表；提取公共的 `_execute_tool_loop()` 消除 ~190 行重复代码，并支持未启用工具的安全拦截。
- **工具循环重写**：`_execute_tool_loop()` 改为必须收到 `final_response` 才返回，业务工具执行与最终输出分离，达到最大轮数或缺少结构化参数时抛错并触发 `retry_async` 重试；移除最大轮数后纯文本降级路径。
- **纯聊天也走工具路径**：`generate_reply()` 不再调用 `generate_text_with_messages()`，统一走 `FINAL_RESPONSE_TOOL` 工具循环（`max_tool_rounds=2`），旧 `messages=None` 的兼容路径现在显式抛错。
- **`generate_reply_with_tools()` 自动追加 `final_response`**：调用方只传入业务工具（read_image / search_web），内部自动追加 `FINAL_RESPONSE_TOOL`；`response_format` docstring 修正以反映非空时实际下放到底层 LLM API 的事实。
- **锁保护范围扩展**：互动历史写入使用现有 `INTERACTION_USER` 作用域，按 `(group_id, user_id)` 在 PG advisory lock 内完成完整 read-modify-write，避免同一用户的并发回复互相覆盖。

### Removed

- **旧互动历史 advisory lock**：`_write_interaction_history()` 不再依赖 PostgreSQL `INTERACTION_USER` 锁，旧锁路径已移除。
- **旧 JSONB records 写入入口**：`MemoryService.upsert_interaction_history()` 已停用（保留签名），`EntityRepository` 旧 interaction 方法保留仅供旧测试渐进清理。
- **旧画像解析**：移除 `summary_worker._apply_profile_operations()`、旧 `user_profile_operations` 解析与聚合逻辑，画像不再通过旧 JSON 操作数组写入。
- **画像总结全量注入**：`llm_service._build_existing_context_with_budget()` 不再把已有画像全量注入总结 prompt，改由 Agent 通过 `read_profile` 按需懒加载；后续该函数及其调用链已完全删除。
- **互动历史总结路径**：`perform_summary()` 中移除互动历史读取、LLM `user_interaction_operations` 处理、`upsert_interaction_history()` 写入；`summarize_conversation()` 不再产出互动历史操作，总结专注于纯对话记忆。
- **总结分段/合并机制**：移除 `_chunk_formatted_messages()`、`_split_oversized_message()`、`_build_merge_prompt()`、`_serialize_chunk_results_for_merge()` 等全部分段与二次汇总逻辑；当前 500 条消息上限远低于模型 40K+ 上下文窗口，分段属过度设计。
- **旧模板字段**：移除 `summary_prompt`、`merge_prompt`、`existing_context_instruction_block`、`existing_profiles_header`、`existing_interactions_header`、`truncated_context_marker`，新增 `summary_workflow_system` 与纯 `memories` JSON 示例。
- **XML 标签提取**：移除 `komari_chat/llm_service._extract_tag_content()`（`komari_memory` 与 `group_history_summary` 中同名函数不受影响）。
- **旧 dispatch 回调**：`generate_reply_with_tools()` 内联的 `dispatch` 回调被 `_execute_business_tool()` helper 取代。
- **独立 LLM 文本压缩路径**：移除 `compact_profile_with_llm()`、`_enforce_profile_trait_limit()` 及配套的 prompt 构建、JSON 解析、分批压缩等全部函数与 Protocol；`profile_compaction.py` 精简为纯规范化与统计工具。
- **`summary_chunk_token_limit` 配置**：直接删除 schema 字段、运行时 JSON、example、README 和测试中的全部残留引用。
- **离线画像瘦身脚本**：删除 `scripts/compact_komari_memory_profiles.py` 及其测试，功能已迁移至 Agent 会话内压缩。

### Fixed

- **陈旧 `access_boost` 测试断言**：`test_memory_service` 两处搜索/触摸调用断言中移除已废弃的 `access_boost` 参数，匹配实际方法签名。

---

## [v1.2.0-beta.4] - 2026-05-28

### Added
- **表情反应提示**：`komari_chat` 判定回复后自动对用户消息发送表情反应（NapCat `set_msg_emoji_like`），示意正在生成回复。通过 `face_reaction_enabled` / `face_reaction_id` 配置项控制，默认关闭，reaction 失败不影响回复流程。
- **联网搜索插件**：新增 `komari_search`，封装 Tavily Search API，供 `komari_chat` 通过 `search_web` 工具调用实现联网搜索。搜索与图片读取工具可在同一轮 tool-calling 循环中共存，不影响总结功能。
- **V4 多模态模式**：`komari_chat` 支持通过工具调用读取图片，实现图文混合对话能力。
- **消息缓冲重构**：`komari_memory` 改用空闲超时 + 最小消息数替代旧的 token 计数机制来触发总结，prompt 构建顺序也做了调整。
- **知识库提案插件**：新增 `komari_custom`，提供 `.custom` 指令组，允许群成员通过多步编辑流程发起知识库提案，经群消息表情投票通过后自动写入 `komari_knowledge` 知识库。默认关闭，需 PostgreSQL + Redis 支持。包含标题正文编辑、撤销、分页列表、投票计数、过期自动清理等功能。
- `AGENTS.md`：新增 AI 智能体上下文文档，描述项目架构、插件依赖、编码规范和开发流程。

### Changed
- **知识库提案提交者展示**：`komari_custom` 投票消息与提案详情中的提交者现在展示名称与 QQ 号，名称优先级为绑定名称 > 用户名 > 群昵称，并持久化到提案表。
- **KnowledgeCategory 扩展**：`komari_knowledge` 模型的 `KnowledgeCategory` 新增 `"custom"` 分类字面量，用于提案通过后写入知识库。
- **依赖更新**：不再直接计算 token 数量，转而使用 `summary_max_buffer_size` 配置项控制总结触发窗口。
- **状态查询迁移**：移除机器人端 `komari_status` / `.status` Uptime Kuma 查询功能，状态查询已迁移至 NapCat 端侧。

### Fixed
- **总结请求误拦截**：修复 `group_history_summary` 中误用 `meaningful_score` / `noise_score`（记忆价值判定）拦截总结意图的问题，移除该错误分支，总结请求只依赖 scene 识别 + 关键词双重门控。
- **修复 Github Action 工作流**: 修复部署包中 Docker 镜像标签包含 v 前缀的问题。

### Test
- 更新 `komari_memory` 和 `komari_chat` 的测试用例，适配上述重构变更。

---

## [v1.2.0-beta.3] - 2026-04-25

### Added
- **LLM 推理内容日志**：`llm_provider` 在调用日志中记录并展示 DeepSeek 的 `reasoning_content` 推理内容。
- **DeepSeek V4 角色扮演指令**：`komari_chat` 新增 V4 角色扮演指令注入功能，重构消息构建结构以适配 KV Cache。
- **群聊总结增强**：`group_history_summary` 集成 DeepSeek V4 角色扮演指令与 `assistant` 预填充配置项。

### Changed
- `group_history_summary` 拆解总结请求判定逻辑，添加详细调试日志。
- `group_history_summary` 将规划阶段系统提示词抽取到配置文件并添加默认值。

### Fixed
- `komari_decision` 补充 `komari_memory` 插件依赖声明，确保插件正确加载顺序。

### Docs
- 补充 `assistant` 预填充与 DeepSeek V4 角色扮演指令的配置示例项。

---

## [v1.2.0-beta.2] - 2026-04-23

### Added
- **维护通知推送**：`komari_management` 新增维护通知推送接口与群组查询 API。
- **状态监控插件**：新增 `komari_status`，集成 Uptime Kuma API 实现服务状态查询。

### Changed
- `komari_help` 命令重命名为 `docs`，新增列表分页功能。
- `komari_help` 提取渲染逻辑至独立模块，新增 `max_reply_result_count` 配置项。
- `komari_help` 新增按插件名称删除自动生成帮助文档的方法，同步时自动清理已禁用插件的帮助条目。
- `komari_help` 新增 `disabled_auto_generate_plugins` 配置项，支持跳过指定插件的帮助文档同步。

### Fixed
- 修正帮助文档命令前缀为点号格式，优化日志字符串格式化。

---

## [v1.2.0-beta.1] - 2026-04-21

### Added
- **健康检查插件**：新增 `komari_healthcheck`，提供无认证状态探测端点。
- `komari_management` 配置 API 响应新增字段描述信息，便于前端展示。
- `decision` 插件新增示例配置文件。

### Changed
- `komari_help` 优化自动帮助条目同步逻辑，避免重复更新并减少索引重建开销。
- `jrhg` 移除独立的 LLM 调用与提示词构建模块，简化插件架构。
- **好感度系统迁移**：好感度功能从 `jrhg` 迁移至 `komari_chat`，统一等级划分标准。
- **数据库配置统一**：Redis 连接配置统一至共享配置，移除各插件冗余的数据库配置覆盖机制。

### Fixed
- `sr` 插件补充插件描述与使用说明，统一指令前缀为点号格式。

### Style / Refactor
- `komari_help` 日志格式化统一改为 f-string 语法。
- `komari_help` 调整 `TYPE_CHECKING` 导入结构，细化序列类型提示为 `list`。

### Docs
- `character_binding`、`jrhg` 优化插件使用说明，统一多行指令展示风格。
- 新增 `komari_help` 插件实现帮助文档向量检索与统一管理 API 集成的文档。

---

## [v1.1.0] - 2026-04-19

> **首个正式发布的 v1.1 稳定版**，合并自 v1.1.0-rc.2。

### Added (自 v1.0.0 以来的累积变更)
- **配置管理接口**：新增配置管理 REST API 并补全测试。
- **DeepSeek extra_params**：`llm_provider` 为 DeepSeek 客户端添加 `extra_params` 自定义参数支持。
- **提示词模板化**：记忆确认与 CoT 前缀角色提取为可配置项。
- **工具调用能力**：`llm_provider` 统一接口新增工具调用能力；群聊总结插件同步接入工具调用。
- **实体增量更新**：实体压缩从全量改写改为增量更新方案。
- **忘却模糊化优化**：修复忘却模块数据类型错误，新增模糊化处理并发功能。
- **角色名绑定**：总结前自动查询群内角色名绑定，优化提示词效果。

### Changed (自 v1.0.0 以来的累积变更)
- **LLM 接口迁移**：从自建客户端迁移至 `openai` 库，统一接口。
- **插件解耦**：`decision` 与 `memory` 插件通过 NoneBot2 标准插件间引用方式解耦。
- **配置解耦**：`decision` 与 `memory` 插件配置完全独立。
- **数据库增量写入**：优化数据库写入为增量方案，并添加迁移脚本。
- **日志级别调整**：部分循环监听日志从 INFO 下调为 DEBUG。

### Fixed
- 早期版本的多个 message/onebot 类型注解问题。
- 停用 LLM 请求中的 `response_format` 字段以避免兼容性问题。
- 图片下载器 EOF 读取和重试处理。
- Sentry 日志过滤与初始化逻辑完善。

### Removed
- **移除本地向量支持**，统一使用远程 embedding 服务。
- **移除内置 WebUI**，使用 REST API 暴露外部管理接口。
- 删除过时的 `not_related` 空回功能。

---

## [v1.1.0-rc.2] - 2026-04-18

### Changed
- 优化提示词。
- 记忆确认与 CoT 前缀角色提取为可配置项。

### Fixed
- 修复 `deepseek_client` 额外参数可能覆盖 SDK 请求字段的问题。

---

## [v1.1.0-rc.1] - 2026-04-18

### Added
- `llm_provider` 为 DeepSeek 客户端添加 `extra_params` 自定义参数支持。

---

## [v1.1.0-beta.7] - 2026-04-14

### Added
- 新增配置管理接口并补全测试。

### Fixed
- 修复 `komari_decision` 插件根模块未导出服务类的问题。
- 修复配置 schema 命名不统一的问题。

---

## [v1.1.0-beta.6] - 2026-04-13

### Changed
- 部分循环监听日志下调为 DEBUG 级别。
- 更新示例配置文件。
- 聊天记录总结插件与 chat 插件改用标准 NB2 插件间引用方法。

---

## [v1.1.0-beta.5] - 2026-04-12

### Added
- 实体压缩改为增量更新方案。
- 为忘却模块新增模糊化处理并发功能。

### Fixed
- 修正忘却模块因数据类型错误无法正常工作的问题。
- 修复 management 日志格式问题。

---

## [v1.1.0-beta.4] - 2026-04-12

### Added
- 新增总结前查询角色名绑定。
- 优化提示词。
- 数据库写入优化为增量写入，添加迁移脚本。

### Fixed
- 修正忘却模块数据类型错误问题。

---

## [v1.1.0-beta.3] - 2026-04-11

### Added
- 新增独立管理页面插件，统一挂载管理接口与 Swagger/OpenAPI。
- 交互历史改为增量追加。
- 新增回复日志本地管理 REST 接口。
- 新增记忆库本地管理 REST 接口。

### Changed
- 重构知识库管理接口共享鉴权逻辑。
- 简化日志记录，仅记录回复文本。
- 移除内部 WebUI，使用 REST API 暴露外部管理接口。

### Removed
- 删除过时配置。

### Test
- 迁移单元测试至 NoneBug 框架。

---

## [v1.1.0-beta.2] - 2026-04-09

### Added
- 为 `jrhg` 新增独立配置。

---

## [v1.1.0-beta.1] - 2026-04-08

### Added
- `jrhg` 请求 LLM 生成问好回复。
- 支持引用消息注入 `komari_chat` 上下文功能。

### Fixed
- 修复提示词中 XML 标签格式问题。

---

## [v1.0.0] - 2026-03-31

> **首个稳定版（v1.0.0）发布**。

### Core
- **NoneBot2** 框架 + **OneBot V11** 适配器，QQ 机器人基础架构。
- **插件体系**：18 个插件，分为基础服务层、核心功能层、辅助功能层。

### Major Features
- **AI 聊天**（`komari_chat`）：LLM 驱动的对话能力，支持 @触发、主动回复。
- **四层记忆系统**（`komari_memory`）：对话摘要、用户画像、互动历史、实体知识。
- **RAG 知识库**（`komari_knowledge`）：向量检索 + 关键词匹配，提供 REST 管理 API。
- **智能帮助**（`komari_help`）：基于向量检索的帮助文档系统。
- **群聊总结**（`group_history_summary`）：@机器人后自动总结聊天记录，图片输出。
- **好感度系统**：`.jrhg` 命令，LLM 生成个性化问好。
- **角色绑定**：`.nn` 指令，自定义群内昵称。
- **神人榜**：`.sr` 指令，随机抽取群友。
- **Sentinel/运维**：Sentry 集成、Bark 推送、健康检查。

### Infrastructure
- **PostgreSQL + pgvector**：HNSW 向量索引，raw SQL（无 ORM）。
- **Redis 7.1+**：`redis.asyncio` 异步客户端。
- **FastAPI**：内嵌于 NoneBot2 的管理 API。
- **Docker + Docker Compose**：容器化部署，Gunicorn + Uvicorn。
- **GitHub Actions**：CI/CD 自动构建 Docker 镜像。

### Dev
- Python **3.13+** 专属，零容忍类型错误（Pyright `standard` + Ruff）。
- Poetry 包管理。

---

## [v0.7.0] - 2026-03-05

> v1.0.0 之前的最后一个 milestone 版本，奠定了核心架构。

### Features
- 知识库注入（轻量 RAG + 关键词检索）。
- 四层记忆系统初版。
- AI 聊天增强（BERT 模型主动回复判定）。
- `.nn` 指令复刻（自定义昵称）。
- SR 插件重构（神人榜抽签）。
- Bot 上下线 Bark 推送。
- 独立的权限、配置、数据库管理插件。
- 可自定义字体，图片布局配置驱动。

### Changed
- 统一数据库接入接口。
- 总结只传递纯文字。
- 提示词持续优化。
- Docker 部署优化（dockerignore、action 构建修复）。

### Fixed
- 总结时触发主动回复的问题。
- 总结触发词优化，添加超出范围提醒。

---

## 版本对照表

| 版本 | 日期 | 类型 |
|------|------|------|
| Unreleased | - | dev 分支 |
| v1.2.0-beta.4 | 2026-05-28 | Beta |
| v1.2.0-beta.3 | 2026-04-25 | Beta |
| v1.2.0-beta.2 | 2026-04-23 | Beta |
| v1.2.0-beta.1 | 2026-04-21 | Beta |
| v1.1.0 | 2026-04-19 | **Stable** |
| v1.1.0-rc.2 | 2026-04-18 | RC |
| v1.1.0-rc.1 | 2026-04-18 | RC |
| v1.1.0-beta.7 | 2026-04-14 | Beta |
| v1.1.0-beta.6 | 2026-04-13 | Beta |
| v1.1.0-beta.5 | 2026-04-12 | Beta |
| v1.1.0-beta.4 | 2026-04-12 | Beta |
| v1.1.0-beta.3 | 2026-04-11 | Beta |
| v1.1.0-beta.2 | 2026-04-09 | Beta |
| v1.1.0-beta.1 | 2026-04-08 | Beta |
| v1.0.0 | 2026-03-31 | **Stable** |
| v0.7.0 | 2026-03-05 | Milestone |

---

*最后更新：2026-06-03（Unreleased 按三大主题重组）*
